import React from "react";export default function Privacy(){return <div className="page"><div className="eyebrow">LEGAL INFORMATION</div><h1 className="title">Privacy</h1><p className="sub">This page describes the data the current WyBuild implementation is designed to process. Publish a reviewed legal policy before commercial launch.</p><div className="card"><h3>GitHub data</h3><p className="muted">WyBuild receives GitHub identity information and access needed to list repositories, branches and GitHub Actions resources that you authorize. Source code remains on GitHub except where a specific build workflow processes it inside GitHub Actions.</p><h3>Application data</h3><p className="muted">WyBuild may store project, build, release, usage and account metadata required to operate the service. Temporary artifacts should be expired according to the configured retention policy.</p><h3>Billing</h3><p className="muted">Flutterwave payment processing and subscription verification are handled by WyDev. WyBuild does not receive or store payment-card credentials.</p><h3>Security</h3><p className="muted">GitHub access tokens are kept server-side in an HttpOnly session cookie and are not exposed to browser JavaScript. Signing credentials must remain in encrypted CI secrets.</p></div></div>}
