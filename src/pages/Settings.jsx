import React,{useState} from 'react';import {githubLogout} from '../github';export default function Settings({session,onLogin}){const[error,setError]=useState('');const[busy,setBusy]=useState(false);const disconnect=async()=>{setBusy(true);setError('');try{await githubLogout();location.reload()}catch(e){setError(e.message)}finally{setBusy(false)}};return <div className="page"><div className="eyebrow">ACCOUNT</div><h1 className="title">Settings</h1><div className="card"><h3>GitHub</h3><p className="muted">{session?`Connected as @${session.user.login}.`:'Not connected.'}</p><button className="btn secondary" disabled={busy} onClick={session?disconnect:onLogin}>{busy?'Disconnecting…':session?'Disconnect GitHub':'Connect GitHub'}</button>{error&&<div className="notice error">{error}</div>}</div><div className="card"><h3>Security</h3><p className="muted">GitHub tokens are held in an HttpOnly session cookie and are never rendered into frontend JavaScript.</p></div></div>}
