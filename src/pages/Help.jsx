import React from "react"; import {Link} from "react-router-dom";
export default function Help(){return <div className="page"><div className="eyebrow">SUPPORT</div><h1 className="title">Help</h1><p className="sub">Recovery paths for the most common WyBuild problems.</p><div className="doc"><h2>GitHub connection failed</h2><p>Check App credentials, callback URL, permissions and whether access was revoked. Reconnect GitHub after correcting the configuration.</p></div><div className="doc"><h2>Build failed</h2><p>Open the raw workflow logs first. Compare the failing stage with the deterministic troubleshooting guidance in Docs.</p></div><div className="doc"><h2>Billing problem</h2><p>Payment receipt and subscription state must be verified server-side. A successful browser redirect alone does not unlock paid features.</p></div><Link className="btn secondary" to="/docs">Open Docs & Guide</Link></div>}